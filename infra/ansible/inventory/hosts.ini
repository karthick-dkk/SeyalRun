[jumpserver]
192.168.64.2 ansible_user=test ansible_become=yes ansible_become_pass={{ vault_sudo_pass }}

[zabbix]
# Server 2 is currently unreachable (wrong subnet / VM offline)
# Uncomment when Server 2 is online:
# 192.168.43.4 ansible_user=test ansible_become=yes ansible_become_pass={{ vault_sudo_pass }}

[freeipa]
# FreeIPA will be installed on Server 1 (co-located with JumpServer for lab)
192.168.64.2 ansible_user=test ansible_become=yes ansible_become_pass={{ vault_sudo_pass }}

[lab:children]
jumpserver
freeipa

[all:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
